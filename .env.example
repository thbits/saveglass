# AI Agents Playground Environment Configuration
# Copy this file to .env and fill in your actual values

# OpenAI Configuration (Default Provider)
OPENAI_API_KEY=your_openai_api_key_here

# AWS Bedrock Configuration (Alternative Provider)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
# AWS_SESSION_TOKEN=your_session_token_here  # Only needed for temporary credentials

# Optional: AWS Profile (alternative to access keys)
# AWS_PROFILE=your_aws_profile_name

# Streamlit Configuration (optional overrides)
# STREAMLIT_SERVER_HEADLESS=true
# STREAMLIT_SERVER_PORT=8501
# STREAMLIT_SERVER_ADDRESS=0.0.0.0

# Development Settings
# PYTHONPATH=/app/src
# PYTHONUNBUFFERED=1

# Notes: 
# - OpenAI API key is required for the default provider (OpenAI GPT models)
# - AWS credentials are only needed if you want to use AWS Bedrock models
# - The application will run in demo mode if no API credentials are provided
# - For AWS Bedrock: Make sure your AWS account has access to Amazon Bedrock
# - Keep your .env file secure and never commit it to version control